import java.text.SimpleDateFormat
    @Library('shared-library@master')
    
    def trigger = new shared-library()
node{
    properties([
        parameters([
            string(name: 'app_branch')
          
        ])
    ]) 

        if("${app_branch}"){
            trigger.buildNdeploy()
        }
}